nav_closeui
The close (X) button for the UI overlay that appears after pressing E in the
crafting area. Clicked (after parking the cursor at the top-left) to dismiss it
before the crafting menu is used. If absent, the step is skipped.

Drop one or more PNG crops here (any filename ending .png).
